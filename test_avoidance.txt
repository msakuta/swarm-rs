tree main = Sequence {
    PredictForward (output -> pos, distance <- "10")
    NewPosition (x <- "80", y <- "64", output -> pos)
    Avoidance (goal <- pos)
    ReactiveSequence {
        FollowPath
        Timeout (time <- "100")
    }
}
